# PostgreSQL connection string. Neon free tier or any PostgreSQL 14+ instance works.
DATABASE_URL="postgresql://neondb_owner:npg_SdfMAT9i7BJO@ep-odd-truth-aymp6jzd.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Used by Better Auth to sign sessions. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET="replace-with-a-long-random-secret"

# The canonical URL for this deployment.
BETTER_AUTH_URL="http://localhost:3000"

# Required for the development seed. Use a local-only password of at least 12 characters.
SEED_ADMIN_PASSWORD="2wsx3edc@WSX#EDC"
